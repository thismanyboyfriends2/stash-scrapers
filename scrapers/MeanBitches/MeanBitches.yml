name: MeanBitches
# requires: py_common
# ignore: *.html cache.json __pycache__ .gitignore requirements.txt

sceneByURL:
  - action: script
    url:
      - megasite.meanworld.com/scenes/
    script:
      - python3
      - MeanBitches.py
      - scene-by-url
galleryByURL:
  - action: script
    url:
      - megasite.meanworld.com/scenes/
    script:
      - python3
      - MeanBitches.py
      - gallery-by-url
sceneByName:
  action: script
  script:
    - python3
    - MeanBitches.py
    - scene-by-name
sceneByQueryFragment:
  action: script
  script:
    - python3
    - MeanBitches.py
    - scene-by-query-fragment
sceneByFragment:
  action: script
  script:
    - python3
    - MeanBitches.py
    - scene-by-fragment

# Last Updated November 7, 2025
